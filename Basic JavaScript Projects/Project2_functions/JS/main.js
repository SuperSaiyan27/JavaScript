//main.js

//a function that ueses the += operator
function myFunction() {
    var sentence = "Hi!, My names Goku"; //creating the variable
    sentence += "I heard you are very strong!"; //using += operator to add the variable
    document.getElementById("Concatenate").innerHTML = sentence; //using document.getElementId()
}